name: freedesktop_file_parser
description: Implementation of the freedesktop.org association between MIME types and applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  result:
    path: ../result

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
